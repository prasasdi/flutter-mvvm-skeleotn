import 'package:flutter/material.dart';
import 'package:get/get.dart';

class DummyView extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('implikasi'),
    );
  }
}